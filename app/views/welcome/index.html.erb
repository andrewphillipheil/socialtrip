<% unless user_signed_in? %>
	<div class="hero-unit margin100">
	  <h1>Welcome to Tripize</h1>
	  <p>Andrew Heil</p>
	  <p>
		  <a class="btn btn-primary btn-large" heef="#">Learn more</a>
	  </p>
  </div>
<% else %>
	<div class="container" style="margin-top:85px;">
		<div class="row">
			<div class="span6"></div>
			<div class="span6">
				<div class="user-actions pull-right">
					<% if user_signed_in? %>
					  Welcome To SocialTrip
	          <br/>
	          <%= link_to "Manage Trips", trips_path %>
					<%else%>
					  <%= link_to "Sign In", new_user_session_path %>
	          <%= link_to "Sign Up", new_user_registration_path %>
					<%end%>
				</div>
				
			</div>
		</div>
	</div>
<% end %>


