<table class='table table-striped table-bordered table-condensed'>
  <tr>
    <th>Name</th>
    <th>Destination</th>
    <th>Start Date</th>
    <th>End Date</th>
    <th>Description</th>
    <th colspan='3'>Actions</th>
  </tr>

  <% @trips.each do |trip| %>
    <tr>
      <td><%= trip.name %></td>
      <td><%= trip.destination %></td>
      <td><%= trip.start_date %></td>
      <td><%= trip.end_date %></td>
      <td><%= trip.description %></td>
      <td><%= link_to "Show", trip_path(trip)  %></td>
      <td><%= link_to "Edit", edit_trip_path(trip)  %></td>
      <td><%= link_to "Destroy", trip_path(trip), :method => :delete, :confirm => 'Are you sure?'  %></td>
    </tr>
  <% end %>
</table>

<%= link_to "Create new trip", new_trip_path %>

